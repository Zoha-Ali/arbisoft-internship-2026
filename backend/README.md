# Backend

Not started yet — scaffolded in Week 2 (Backend, REST, CRUD & ORM).

Planned: a CRUD REST API for one resource (e.g. Notes or Tasks), modeled with an ORM
(Django ORM / SQLAlchemy / Prisma), with input validation and a clean linter pass. See the
root `CHECKLIST.md` for the full Week 2 list.
