const About = () => {
  return (
    <section>
      <h1>About This Project</h1>
      <p>
        The Todo app lets you add, complete, and delete tasks — built to explore core React concepts
        in a practical setting. It uses lifting state up to share todos across components,
        controlled inputs for the add-task form, and inline validation to prevent empty submissions.
        The project also showcases client-side routing with React Router, component composition, and
        TypeScript interfaces to keep the data shape explicit throughout the app.
      </p>
    </section>
  );
};

export default About;
